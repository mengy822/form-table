import { createApp } from 'vue'
import App from './App.vue'
import 'element-plus/dist/index.css'
// import detail from './views/package/components/Detail/detail'.use(detail)
createApp(App).mount('#app')
